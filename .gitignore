# Logs
logs
*.log
npm-debug.log*
yarn-debug.log*
yarn-error.log*
lerna-debug.log*
.pnpm-debug.log*

# Diagnostic reports
report.[0-9]*.[0-9]*.[0-9]*.[0-9]*.json

# Runtime data
pids
*.pid
*.seed
*.pid.lock

# Coverage
coverage
*.lcov
.nyc_output

# Dependency directories
node_modules/
node_modules.zip
jspm_packages/

# TypeScript
*.tsbuildinfo
dist
dist.zip

# dotenv / secondary env files
.env
.env.*.local
.env.local

# Secret app config (Token, Mongo URI, Spotify/Genius, web auth)
# — never commit/push
app.yml
app.yml.*.bak
app.yml.*.secret
ZamptoConfig.md

# PM2 config (contains hardcoded keys)
ecosystem.config.cjs

# Deploy artifact — rynote.zip bundles app.yml (contains real secrets)
rynote.zip
*-deploy.zip
lavalink-bot-hosting.zip

# package-lock.json intentionally not versioned (generated by npm install for deploy.sh)
package-lock.json

# Databases (live state)
cylane.database.json
byteblaze.database.json
.cylane/

# Lavalink (large jar + plugins)
Lavalink/
/opt/lavalink2/

# Temp / scratch
draft/
temp/
.clinic
test.*************

# Internal files
AGENTS.md
scripts/deploy.sh